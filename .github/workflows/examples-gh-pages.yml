# This action builds the provided examples with Emscripten and publishes them on GitHub pages.

name: examples-gh-pages

on:
  push:
    branches: [ "main" ]

env:
  EM_VERSION: 5.0.1
  EM_CACHE_FOLDER: 'emsdk-cache'

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup cache
        id: cache-system-libraries
        uses: actions/cache@v5
        with:
          path: ${{env.EM_CACHE_FOLDER}}
          key: ${{env.EM_VERSION}}-${{ runner.os }}

      - name: Setup emsdk
        uses: emscripten-core/setup-emsdk@v16
        with:
          version: ${{env.EM_VERSION}}
          actions-cache-folder: ${{env.EM_CACHE_FOLDER}}

      - name: Configure cmake
        run: emcmake cmake -S dev -B build -DBUILD_EXAMPLES=ON

      - name: Build examples
        run: cmake --build build

      - name: Create index.html
        run: |
          cd ./build/examples
          touch index.html
          echo '<!doctype html><html lang=en><head><meta charset=utf-8><title>PGE3 Examples</title></head><body><h1>PGE3 Examples</h1><ul>' >> index.html
          ls ./build/examples/*.html | xargs printf '<li><a href=\"%s\">%s</a></li>' >> index.html
          echo '</ul></body></html>' >> index.html
          cd -

      - name: Setup GitHub Pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v4

      - name: Upload build artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./build/examples"

  deploy:
    runs-on: ubuntu-24.04
    needs: build
    
    permissions:
      pages: write
      id-token: write

    environment:
      # environment created automatically by GitHub
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
